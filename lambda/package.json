{
  "name": "limitless-trade-generator",
  "version": "1.0.0",
  "description": "Lambda function to fetch Limitless Exchange data and generate trade files for S3",
  "main": "index.js",
  "scripts": {
    "test": "node test.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.621.0",
    "ethers": "^6.11.1"
  },
  "engines": {
    "node": ">=18.x"
  }
}
